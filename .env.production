# Used automatically when you run `npm run build`
# Point this at your LIVE backend (keep it relative if the frontend is served
# from the same origin as the backend, or replace it with your real API domain).
VITE_API_BASE_URL=https://shrishahuprabodhini.in/api
VITE_APP_ENV=production
